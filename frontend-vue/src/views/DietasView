<template>
  <div class="container">
    <h2>Mis Dietas</h2>
    <div class="dietas-grid">
      <div class="dieta-card" v-for="i in 3" :key="i">
        <h3>Dieta {{ i }}</h3>
        <p>Próximamente...</p>
      </div>
    </div>
  </div>
</template>

<style scoped>
.container {
  padding: 20px;
  color: white;
}
.dietas-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 20px;
  margin-top: 20px;
}
.dieta-card {
  background: white;
  color: #333;
  padding: 20px;
  border-radius: 15px;
}
</style>